{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "prompt",
        "timeout": 10,
        "prompt": "When a session starts in this repo, ask the user: \"Good day. Shall I prepare your brief?\" If the user agrees, run `forge brief --paths --path-format absolute` and then produce the assistant-style brief using the prompt embedded in AGENTS.md. Hook input: $ARGUMENTS"
      }
    ]
  }
}

