#!/usr/bin/env bash
# forge-refresh.sh — Retired OmniFocus snapshot refresh
# Kept as a no-op for existing cron/LaunchAgent references after migration to SP.
#
# OmniFocus is no longer part of the daily workflow; SP is authoritative.

set -euo pipefail

echo "forge-refresh: OmniFocus refresh disabled; Super Productivity is authoritative" >&2
