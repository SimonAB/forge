# Hermes + Forge

Plug-and-play local assistant setup for this repository.

```bash
python3 scripts/setup-hermes-forge.py
```

Full guide: **[`docs/hermes.md`](../docs/hermes.md)**

Forge.app → Preferences → **Hermes** — check status, run setup, open the guide.

Start: `hermes` then `/skill:forge-board` (read-only briefs and board reads need no approval; column/tag changes require explicit confirmation per `AGENTS.md`).
